"""
Defines some behavioural classes that we can use to define basic roles in a circuit.

These are actually stored in the BaseClasses directory, but this file is included as a convenient conglomeration of them. A little odd, but I think it should help address the fact that these classes are both useful objects that do something, and conceptual classes that represent ideas.
"""

from BaseClasses.Connector import Connector
from BaseClasses.Junction import Junction
